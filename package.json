{
  "name": "acydion-resilient-scraper",
  "version": "1.0.0",
  "description": "Production-grade resilient scraper and anti-bot bypass ingestion pipeline with real-time telemetry dashboard",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "server": "node server/server.js",
    "client": "npm --prefix client run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "npm --prefix client run build",
    "install-all": "npm install && npm --prefix server install && npm --prefix client install",
    "test": "node server/tests/verify-pipeline.js"
  },
  "keywords": [
    "scraper",
    "anti-bot",
    "circuit-breaker",
    "resilience",
    "telemetry"
  ],
  "author": "Acdyon Challenge Candidate",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^9.1.2",
    "serverless-http": "^4.0.0"
  }
}
